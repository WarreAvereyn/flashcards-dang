services:
  postgres:
    image: postgres:16
    container_name: flashcards-db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - flashcards_pgdata:/var/lib/postgresql/data

volumes:
  flashcards_pgdata:
